'use client'

export function DashboardJs() {
  return (
    (
    <main className="flex-1 p-8">
        <h2 className="text-3xl font-semibold text-gray-800">Welcome to Your Dashboard</h2>
        <p className="mt-4 text-gray-600">Select a options from the sidebar to manage users, roles, permissions, projects, and modes.</p>
      </main>    )
  );
}